import React from 'react';
import PropTypes from 'prop-types';


const AttendeeBox = ({ status, step, username }) => (
  <div>
    BOX
  </div>
);

AttendeeBox.propTypes = {
  
};

export default AttendeeBox;